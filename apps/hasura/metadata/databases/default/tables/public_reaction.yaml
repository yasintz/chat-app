table:
  name: reaction
  schema: public
select_permissions:
  - role: customer
    permission:
      columns:
        - name
        - id
      filter: {}
  - role: member
    permission:
      columns:
        - name
        - id
      filter: {}
